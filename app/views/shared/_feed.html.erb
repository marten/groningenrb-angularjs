<% if @feed_items.any? %>
  <ol class="microposts" ng-controller="FeedCtrl" ng-init="feed_items = <%= @feed_items.to_json %>">
    <%= render partial: 'shared/feed_item', collection: @feed_items %>
  </ol>
  <%= will_paginate @feed_items %>
<% end %>